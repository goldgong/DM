<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 

<mapper namespace="dm.mybatis.mapper.DMMapper" > 
	
	<select id="selectDMList" parameterType="dmvo" resultType="dmvo"> 
		SELECT 
		'member id' as memberUid, 
		'nick name' as nickName, 
		'passwd' as passwd, 
		'email' as email
		FROM dual 
	</select> 


</mapper>